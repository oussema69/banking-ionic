import { ContactclientsPipe } from './contactclients.pipe';

describe('ContactclientsPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactclientsPipe();
    expect(pipe).toBeTruthy();
  });
});
